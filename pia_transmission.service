[Unit]
Description=PIA (wg + transmission port forwarding)

[Service]
WorkingDirectory=/usr/bin/pia
ExecStart=/usr/bin/pia/wireguard-start.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
